{
  "name": "trigger-dynamo",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "dependencies": {
    "aws-sdk": "^2.814.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke-local": "sls invoke local -f viking-insert --path requests-mock/vikings.insert.json --log",
    "invoke": "sls invoke -f viking-insert --path requests-mock/vikings.insert.json --log"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}